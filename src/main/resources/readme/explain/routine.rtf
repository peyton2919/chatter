// 路由
根目录: http://localhost:8080/api
    名称              提交方式            controller             路径名称

1. 发送验证码    {method = post}         UserController          /sendcode
2. 用户注册      {method = post}        UserController          /reg
3. 手机登录      {method = post}        UserController          /phonelogin



